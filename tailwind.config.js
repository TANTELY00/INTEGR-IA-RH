/** @type {import('tailwindcss').Config} */
export default {
    darkMode: 'class', // important pour pouvoir basculer si besoin, mais reste en clair par défaut
    content: [
      "./src/**/*.{html,ts}"
    ],
    theme: {
      extend: {
        
      },
    },
    plugins: [],
  }
  